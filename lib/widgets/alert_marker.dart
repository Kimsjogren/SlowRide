import 'package:flutter/material.dart';

class AlertMarker extends StatelessWidget {
  const AlertMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.warning_amber, size: 20);
  }
}
