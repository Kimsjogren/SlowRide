import 'package:flutter/material.dart';

class ConvoyMarker extends StatelessWidget {
  const ConvoyMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.directions_car, size: 20);
  }
}
