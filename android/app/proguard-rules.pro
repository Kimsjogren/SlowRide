# App-specific R8 rules belong here. Flutter and the Android plugins bundled
# with CruizX publish their own consumer rules, so no broad keep rules are
# required. Keeping this file intentionally narrow allows full optimization.
